# {{PROJECT_NAME}} AI 起始指南

## 先讀

1. 根目錄 `AGENTS.md`。
2. 本檔案和 `docs/ai/MULTI_AGENT.md`。
3. 指派成果所依據的已接受規格或架構決定。
4. 目前有效的任務交接、相關原始碼、測試和可重現證據。

聊天記錄和模型記憶只能協助找資料，不能作為專案的最高依據。

## 編輯之前

1. 檢查 Git 狀態、目前分支、最新遠端狀態、進行中的任務和工作區使用情況。
2. 確認明確成果、負責人、目標分支、獨占寫入範圍、共用路徑、依賴、驗證和下一個安全操作。
3. 套用 `{{WORKFLOW_MODE}}` 模式政策。唯讀檢查不需要額外的任務認領流程。
4. 範圍或具優先效力的文件互相矛盾時停止。

## 工作期間

- 保持檢查點可重現；不要讓唯一可用的狀態只存在於聊天或未提交的工作區中。
- 公開行為或合約改變時，更新規格、測試和證據。
- 只執行小型必跑集合加上變更觸發的條件關卡。
- 用產品語言解釋進度，再提儲存庫操作細節。

## 交接之前

記錄變更內容、實際驗證結果、限制、目前負責人和基準，以及一項明確的下一步安全操作。確認前一個寫入者已停止，且記錄狀態已重新讀取後，新寫入者才能開始。
