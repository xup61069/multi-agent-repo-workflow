# {{PROJECT_NAME}} 多代理協作

目前模式：`{{WORKFLOW_MODE}}`。任務追蹤器：`{{TASK_TRACKER}}`。

## 模式行為

{{MODE_POLICY}}

## 嚴格並行切片

啟用嚴格責任劃分時，每個寫入者都要有一個任務、一個明確成果、一個分支、一個交接區塊、一個獨占寫入範圍、一個隔離的 Git 檢出目錄，以及第一個可重現檢查點完成後可供審查的變更。

交接記錄：

- 擁有者、角色、狀態、分支、目標分支和基準 commit；
- `scope_globs`、`shared_paths` 和 `depends_on`；
- 相關驗證命令和證據邊界；
- 一個 `next_safe_action`。

## 範圍與共用路徑

`scope_globs` 表示獨占寫入範圍。`shared_paths` 需要整合者或排定編輯順序；列入共用路徑不代表重疊寫入是安全的。目錄分工只是路由提示，語意合約仍可能跨目錄重疊。

## 交接

目前寫入者建立可重現的檢查點，更新驗證和限制，記錄一個下一安全操作，然後停止寫入。下一個寫入者在編輯前檢查遠端狀態、分支佔用和交接內容。

## 停止條件

遇到下列情況就停止重疊寫入：任務認領重疊、變更超出指派範圍、具優先效力的文件互相矛盾、分支責任不明、缺少外部授權，或操作具破壞性／成本。只向維護者提出繼續作業所需的最小決定。

## 報告

告訴維護者哪些能力改變了、使用者會注意到什麼、實際驗證了什麼，以及還剩哪些問題。只有在影響可信度或下一步時，才附上簡短的 Git 稽核資訊。
